<p id="notice"><%= notice %></p>

<p>
  <strong>Latitude:</strong>
  <%= @event.latitude %>
</p>

<p>
  <strong>Longitude:</strong>
  <%= @event.longitude %>
</p>

<%= link_to 'Edit', edit_event_path(@event) %> |
<%= link_to 'Back', events_path %>

<br>

<%= image_tag google_map([ @event.latitude, @event.longitude ].join(',')) %>
